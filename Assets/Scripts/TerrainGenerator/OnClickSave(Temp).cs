using UnityEngine;

namespace TerrainGenerator
{
    public class OnClickSave : MonoBehaviour
    {
        /*
        public void Update()
        {
            if(Input.GetKeyDown(KeyCode.F3))
            {
                WorldSaveSystem.Instance.SaveWorld();
            }
            if (Input.GetKeyDown(KeyCode.F9))
            {
                WorldSaveSystem.Instance.LoadWorld();
            }
        } */
    }
}
